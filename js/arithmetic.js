function arith() {
 			//  1+2*3-5/6%4
 			var expn=document.getElementById('expn').value;
 			var result = eval(expn);
 			
 			if(result) return result;
 		// 	var Values=[];
 		// 	var Count=0;
 		// 	var Symbol=[];
			// // while(Expression.search('+')>0 ||Expression.search('-')>0 ||Expression.search('*')>0 ||Expression.search('/')>0 ){
 		// 	 // Plus=Expression.search('+');
 		// 	 var i=0;
 		// 	 for ( i = 0; i < Expression.length; i++) 
			// 	if(Expression[i]=='+')
			// 	break;
			// Symbol[0]=i;
			// i=0;
 		// 	 for ( i = 0; i < Expression.length; i++) 
			// 	if(Expression[i]=='-')
			// 	break;
			// Symbol[1]=i;
			// i=0;
 		// 	 for ( i = 0; i < Expression.length; i++) 
			// 	if(Expression[i]=='*')
			// 	break;
			// Symbol[2]=i;
			// i=0;
 		// 	 for ( i = 0; i < Expression.length; i++) 
			// 	if(Expression[i]=='/')
			// 	break;
			// Symbol[3]=i;
			// i=0;
 		// 	 for ( i = 0; i < Expression.length; i++) 
			// 	if(Expression[i]=='')
			// 	break;
			// Modulus=i;

			// var Signs=0;
			// if(Plus>0)Signs++;
			// if(Minus>0)Signs++;
			// if(Product>0)Signs++;
			// if(Division>0)Signs++;
			// if(Modulus>0)Signs++;

			// while(Signs--!=0){

			// }

 			//document.getElementById('Expression').value=Current;
 		}